<h2> <%= @item.name %> </h2>
<h2> <%=  @item.description %>  </h2>
<h2> <%=  image_tag @item.photo %>  </h2>

<%= link_to 'Edit', edit_item_path(@item) %> |  <%= link_to 'Delete', @item, method: :delete, data: { confirm: 'Are you sure you want to delet your item?' } %>